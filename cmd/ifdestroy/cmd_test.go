package ifdestroy

import (
	"testing"
)

func TestRun(t *testing.T) {
	// ifDestroyCmd := Cmd{}

	// buf := bytes.NewBuffer([]byte(""))
	// testCmdCtx := core.CmdCtx{
	// 	StdOut: buf,
	// }

	// ifDestroyCmd.Run(testCmdCtx)

	// output := buf.String()
	// want := "Hello, world!\n"
	// if output != want {
	// 	t.Errorf("Run() got = %s, want %s", output, want)
	// }
}
